#!/bin/sh

cd $HOME/Dropbox/Charles/Muse

echo "Orphaned Muse Pages:"
for i in *.muse
do
  pagename=`basename $i .muse`
  find . -type f ! -name '*~' | xargs -I {} echo \"{}\" | xargs egrep -n -e "$pagename" 2>&1 >/dev/null
  if [ "$?" -ne 0 ]; then
    echo "  $i"
  fi
done

