*

# N.B.: To properly add a directory and all its subdirectories, don't rely on the "**"
# glob that's sometimes seen in gitignore files.  On some platforms (e.g., OS X), that's
# not supported by fnmatch(), so it doesn't work.  Makes things ugly, but portable.

# gitignore for this repo
!.gitignore
!.gitmodules

# My binary directory containing scripts
!bin
!bin/*
bin/genrand
bin/mate
!bin/*/*
!bin/*/*/*
!bin/*/*/*/*
!bin/*/*/*/*/*
!bin/*/*/*/*/*/*
!bin/*/*/*/*/*/*/*
!bin/*/*/*/*/*/*/*/*

# Ignore my generated BibTeX files
bin/texmf/bibtex/bib

# Ignore AuCTeX generated files
.auctex

# Ignore compiled python in all directories
bin/*.pyc
bin/*/*.pyc
bin/*/*/*.pyc

# Shell configuration
!.bashrc
!.bash_profile
!.hushlogin
!.inputrc
!.profile

# XTerm/RXVT
!.Xdefaults

# Ruby
!.gemrc

# Nano
!.nanorc

# Mercurial
!.hgrc

# GTK
!.gtkrc-2.0

# GPG
!.gnupg
!.gnupg/gpg.conf

# SVN
!.subversion
!.subversion/config

# GDB
!.gdbinit

# Lastly, ignore all DS_Store files
*.DS_Store
