*

# N.B.: To properly add a directory and all its subdirectories, don't rely on the "**"
# glob that's sometimes seen in gitignore files.  On some platforms (e.g., OS X), that's
# not supported by fnmatch(), so it doesn't work.  Makes things ugly, but portable.

# gitignore for this repo
!.gitignore
!.gitmodules

# My binary directory containing scripts
!bin
!bin/*
!bin/*/*
!bin/*/*/*

# Ignore compiled python in all directories
bin/*.pyc
bin/*/*.pyc
bin/*/*/*.pyc

# Shell configuration
!.bashrc
!.bash_profile
!.hushlogin
!.inputrc
!.profile

# Emacs configuration
!.emacs

# Emacs lisp files and other site packages
!.emacs.d
!.emacs.d/*
!.emacs.d/*/*
!.emacs.d/*/*/*
!.emacs.d/*/*/*/*
!.emacs.d/*/*/*/*/*
!.emacs.d/*/*/*/*/*/*
!.emacs.d/*/*/*/*/*/*/*
!.emacs.d/*/*/*/*/*/*/*/*
!.emacs.d/*/*/*/*/*/*/*/*/*

# Ignore compiled elisp in all directories
.emacs.d/*/*.elc
.emacs.d/*/*/*.elc

# The following are autogenerated, and/or might have sensitive
# information in them
.emacs.d/auto-save-list
.emacs.d/auctex-var
.emacs.d/recentf

# Terminfo for emacs-term
!.terminfo
!.terminfo/*
!.terminfo/*/*

# Nano
!.nanorc

# GTK
!.gtkrc-2.0

