(
  "Window Maker",
  (Run..., SHORTCUT, "Mod1+F2", EXEC, "%A(Run, Type command:)"),
  (Terminal, EXEC, urxvt),
  (PCManFM, EXEC, pcmanfm),
  (Emacs, EXEC, emacs),
  ("Google Chrome", EXEC, "google-chrome"),
  (
    Applications,
    (
      Productivity,
      (Wunderlist, EXEC, "/usr/local/bin/wunderlist"),
      (Zotero, EXEC, zotero),
      (Writer, EXEC, lowriter),
      (Calc, EXEC, localc),
      (Impress, EXEC, loimpress),
      (Timer, EXEC, pystopwatch),
      (VirtualBox, EXEC, VirtualBox)
    ),
    (
      Network,
      (Thunderbird, EXEC, thunderbird),
      (FileZilla, EXEC, filezilla),
      (Transmission, EXEC, "transmission-gtk")
    ),
    (
      Graphics,
      ("The GIMP", EXEC, gimp),
      (Draw, EXEC, lodraw),
      (Inkscape, EXEC, inkscape)
    ),
    (
      Media,
      (VLC, EXEC, vlc),
      (Handbrake, EXEC, handbrake),
      (Picard, EXEC, picard),
      (Audacity, EXEC, audacity)
    ),
    (
      System,
      (ARandR, EXEC, arandr),
      (Seahorse, EXEC, seahorse),
      (fsv, EXEC, fsv2),
      (Baobab, EXEC, baobab),
      ("Lock Screen", SHORTCUT, "Mod4+l", EXEC, slimlock),
      (
        "Volume Up",
        SHORTCUT,
        XF86AudioRaiseVolume,
        SHEXEC,
        "$HOME/bin/osd-volume plus"
      ),
      (
        "Volume Down",
        SHORTCUT,
        XF86AudioLowerVolume,
        SHEXEC,
        "$HOME/bin/osd-volume minus"
      ),
      (
        "Volume Mute",
        SHORTCUT,
        XF86AudioMute,
        SHEXEC,
        "$HOME/bin/osd-volume mute"
      ),
      (
        "Brightness Down",
        SHORTCUT,
        XF86MonBrightnessDown,
        SHEXEC,
        "$HOME/bin/osd-brightness minus"
      ),
      (
        "Brightness Up",
        SHORTCUT,
        XF86MonBrightnessUp,
        SHEXEC,
        "$HOME/bin/osd-brightness plus"
      )
    ),
    (
      "XDG Menu",
      OPEN_MENU,
      "|| xdg_menu --format WindowMaker --root-menu /etc/xdg/menus/arch-applications.menu"
    )
  ),
  (
    "Window Maker",
    (
      Backgrounds,
      OPEN_MENU,
      "-noext $HOME/Dropbox/Charles/Personal/Wallpaper/1366x768 $HOME/Dropbox/Charles/Personal/Wallpaper/Tiled WITH wmsetbg -u -t"
    ),
    (Configure, EXEC, "/usr/lib/GNUstep/Applications/WPrefs.app/WPrefs"),
    ("Gather Windows", EXEC, "~/bin/gather-windows"),
    (Restart, RESTART)
  ),
  (
    Workspace,
    (Workspaces, WORKSPACE_MENU),
    ("Hide Others", HIDE_OTHERS),
    ("Show All", SHOW_ALL),
    ("Arrange Icons", ARRANGE_ICONS),
    (Refresh, REFRESH),
    ("Save Session", SAVE_SESSION),
    ("Clear Session", CLEAR_SESSION)
  ),
  (
    "End Session",
    ("Log Out", EXIT),
    (Reboot, EXEC, "gksu \"systemctl reboot\""),
    ("Shut Down", EXEC, "gksu \"systemctl poweroff\"")
  )
)
