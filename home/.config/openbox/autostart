# Reset the background
nitrogen --restore &

# Start the compositor
compton -CGb &

# Start a Polkit authentication agent
lxpolkit &

# Start the notification daemon
dunst &

# Start the panel
$HOME/.config/openbox/runbar.sh &

# Start the automounter
(sleep 1 && udiskie -ant2) &
