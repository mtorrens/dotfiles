#!/bin/sh

pacmd set-default-sink crossfeed
pacmd set-sink-port alsa_output.pci-0000_00_1b.0.analog-stereo analog-output-headphones

# Stop the audio player, which will force it to reset sinks
quodlibet --stop
